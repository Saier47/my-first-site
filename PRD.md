# PRD.md｜校园课表 / DDL 助手

> Day 4 产出 · 2026-09-19
> 项目：my-first-site（学期学习项目，Day 7 做 MVP）
> 上一份文档：research.md（Day 3 同类产品研究 + 本期不做边界）
> 这份文档只回答一件事：**本期要做的功能，具体长什么样，怎么算做完。**

---

## 0. 一句话定位

> **一个打开就是「这一周」的网页：左边是固定课程，右边叠加所有 DDL。手动录入 30 秒能录完，不需要账号，不需要联网。**

（这句话不是今天新想的，是 Day 3 调研完之后把「做一个更干净的课表 App」改写成的那句话。PRD 的全部内容都是围着它展开的。）

---

## 1. 目标用户与使用场景

### 1.1 用户是谁

在校大学生，手上同时有三类东西要记：

| 类型 | 特点 | 现在靠什么记 |
| --- | --- | --- |
| 课程 | 固定的、每周重复的 | 课表 App 已经管得很好 |
| DDL（作业 / 实验报告 / 考试） | 一次性的、老师口头说或群里发 | 微信聊天记录里翻，靠脑子记 |
| 班级通知 | 一堆时间点，不一定和自己有关 | 群消息刷过去就没了 |

### 1.2 核心场景（三个，按重要性排）

**场景 A｜晚上规划明天（最高频）**
晚上 11 点躺在床上，想知道「明天有什么课、有什么要交的」，打开浏览器 → 加到主屏的图标 → 一眼看完 → 关掉。全程不超过 10 秒。

**场景 B｜课上记一个新的 DDL**
老师上课说「下周三交实验报告」，掏出手机打开网站 → 点「添加 DDL」→ 填名字和截止时间 → 保存。全程不超过 30 秒。

**场景 C｜开学第一周把课表录进去（一次性）**
开学第一周，照着自己的课表把一学期的课录一遍。这是最累的一次，也是唯一一次 —— 录完之后一学期都不用再管课程。

### 1.3 用户不会做的事

- 不会为了这个网站去注册账号；
- 不会每天手动更新一遍课表；
- 不会在没有 DDL 的时候还特意打开它（这说明它不该发推送骚扰用户）。

---

## 2. 我们本期砍掉了什么

> **这一节就是今天的主任务答案。**
> research.md 第 3 节讲的是「哪个产品改变了我的想法」，这一节讲的是「这些想法被砍掉之后，具体少做了哪些东西」。

### 2.1 砍掉最大的一块：「做一个更干净的课表 App」

**砍的是什么**：原计划做一个以课表为核心的 App，靠「比超级课程表更干净」来赢。

**为什么砍**：因为 **WakeUp 课程表** 已经把这件事做完了 —— 它包体只有 32–34 MB（官方宣称是竞品的 1/10），**免费、无广告、无内购**，还支持 4 种桌面小组件和多种导入方式。
（出处：research.md 第 1.2 节）

**砍掉的代价我认**：这一刀下去，产品从「一个完整的课表工具」缩成了一块补丁。
但如果不砍，我面对的对手是一个免费且口碑不错的成品，我没有任何赢的角度 ——
**「做得更好还便宜点卖」这条路，在对方免费的时候是走不通的。**

**砍完之后剩下什么**：课表降级成**背景板**，占一屏就够；真正要解决的是课表旁边那些**没人管的 DDL**。

### 2.2 逐条翻译：从「不做」变成「界面上看不到」

research.md 第 4 节列了 8 条不做的事。但「不做社交功能」这种话太软了，谁都能说自己做到了。
所以逐条翻译成**能在界面上查岗**的形式 —— 打开页面数一数，就知道有没有偷偷加回来。

| # | 不做的功能 | 翻译成界面上的事实（可查岗） |
| --- | --- | --- |
| 1 | 教务系统导入 / 爬取 | 页面上**不存在**「导入课表」「输入学号」这类按钮和输入框 |
| 2 | 成绩查询、空教室、蹭课、校园社交、二手市场 | 底部导航**只有 2 个**入口（一周视图、课程列表），没有第 3 个 |
| 3 | 账号体系、云同步、多端同步 | 页面上**没有**「登录」「注册」「同步」入口；关掉浏览器再打开，数据还在（因为在本机） |
| 4 | 打包成 App / 上架 / 做小程序 | 交付物就是一个 `.html` 文件，双击能用，不需要安装任何东西 |
| 5 | 推送通知（微信 / 短信 / 系统通知） | 浏览器**不会**弹出「是否允许通知」的授权请求 |
| 6 | 课程冲突检测、智能调课、AI 排课 | 同一时间段录入两门课时，**允许保存**，不弹「冲突警告」 |
| 7 | 换肤、主题、贴纸、自定义背景 | 页面上**没有**设置页、没有主题切换按钮 |
| 8 | 作业文件上传 / 附件管理 | 添加 DDL 的表单里**没有**上传文件的字段 |

**一句话边界**：本期只做「手动录入 + 一周视图 + 课程与 DDL 叠在一起」这三件事。

### 2.3 砍掉这些之后，我接受的代价

砍功能不是没成本的，下面这些是**明知会痛但本期认了**的：

| 代价 | 具体表现 | 什么时候解决 |
| --- | --- | --- |
| 录课表很累 | 开学第一周要手动把一学期的课敲进去 | 一直如此，这是不碰教务系统的必然代价 |
| 换设备数据就没了 | 手机录的，电脑上看不到 | Day 23 接数据库之后 |
| 不会主动提醒 | 忘了打开就还是忘 | 本期不承诺，靠「加到主屏」提高打开率 |
| 只能在同一个浏览器里用 | 换浏览器数据不通用 | 同上，等云同步 |

---

## 3. 功能清单

优先级说明：
- **P0** = 没有它产品就不成立，Day 7 必须做完；
- **P1** = 有了体验才完整，时间够就做；
- **P2** = 明确留到 Day 7 之后的，本期不做。

### F1｜一周视图（P0）

**这一屏是什么**：打开网站看到的第一屏，也是唯一的主视图。

**输入**：本机已保存的所有课程和 DDL（首次打开时为空）。

**处理**：
1. 取当天所在的一周（周一为起点，周日为终点）；
2. 把课程按「星期 + 开始时间」排进去；
3. 把 DDL 按截止时间排进去，**叠在同一根时间轴上**；
4. 高亮今天。

**输出**：
- 纵向列出 7 天，每天一行；当天那一行有视觉区分（加粗 + 左侧色条）；
- 每一行里，课程和 DDL 混在一起按时间先后排；
- 一条至少显示：时间 + 名称。地点为可选项，有则显示；
- 有 DDL 的那天，DDL 要能被一眼认出来，不能和课程长得一样（用不同颜色区分）；
- 已经过期的 DDL 单独做一个区域显示在顶部，**不能悄悄消失**。

**「过期」的判定口径（这里必须写死，否则会有两种都说得通的实现）**：
把 `due_date` + `due_time` 拼成一个完整的**截止时刻**（例如 `2026-09-24 23:59`），
当前时刻**晚于**这个截止时刻，才算过期。

推论（按这个口径，下面三种情况的结果是确定的）：
- DDL 是 9 月 24 日 23:59，现在是 9 月 24 日中午 → **不算过期**，仍留在正常区域；
- DDL 是 9 月 24 日 23:59，现在是 9 月 24 日 23:59 之后 → 算过期；
- 已经勾选 `done = true` 的 DDL → **不进过期区域**（它已经完成了，不是漏掉的），直接按 F3 的规则从一周视图移除、留在列表里。

**这一屏要做到的事**：微信上发来一条「明天交实验报告」，用户打开网站 3 秒内能确认「对，明天有」。

**边界情况**：
- 这一周一条数据都没有 → 显示一句引导语 + 一个「添加」按钮，不能是空白页；
- 只录了课程没录 DDL → 正常显示课程，不报错；
- 一周里同一天有多条 → 按时间排序，全部显示，不做折叠。

---

### F2｜添加 / 编辑 / 删除课程（P0）

**这一屏是什么**：课程列表页 + 一个表单弹层。

**输入**（表单字段，共 5 个，其中 4 个必填）：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 课程名 | 是 | 文本，例如「高等数学」 |
| 星期几 | 是 | 周一 ~ 周日，单选 |
| 开始时间 | 是 | 时间，例如 08:00 |
| 结束时间 | 是 | 时间，必须晚于开始时间 |
| 上课地点 | 否 | 文本，例如「A101」 |

**处理**：
1. 保存时校验必填项，缺一项就提示，不保存；
2. 校验「结束时间 > 开始时间」，否则提示，不保存；
3. 保存成功后表单关闭，列表和一周视图同时更新；
4. 删除时先弹一次确认，确认后才删；
5. **课程按「每周重复」处理** —— 录一次，之后每一周都会出现，不需要每周重录。

**输出**：
- 课程列表页按星期分组显示所有课程；
- 每条课程右侧有「编辑」和「删除」两个入口；
- 编辑时表单里带出原值，改完保存即生效。

**边界情况**：
- 同一时间段录入两门课 → **允许保存，不弹冲突警告**（这是第 2.2 节第 6 条明确砍掉的）；
- 课程名为空 → 不允许保存；
- 时间段跨中午（如 11:00–13:00）→ 正常保存，不做特殊处理。

---

### F3｜添加 / 编辑 / 删除 DDL（P0）

**这一屏是什么**：和课程共用同一个表单弹层的位置，但字段不同。

**输入**（表单字段，共 4 个，其中 3 个必填）：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 事项名称 | 是 | 文本，例如「交数据结构实验报告」 |
| 截止日期 | 是 | 日期，例如 2026-09-24 |
| 截止时间 | 是 | 时间，例如 23:59 |
| 备注 | 否 | 文本，例如「第 3 章之后的题」 |

**处理**：
1. 校验必填项与日期时间格式；
2. 保存后立刻出现在对应那一天的一周视图里；
3. **DDL 按「只出现一次」处理** —— 到期后不再重复，和课程的重复规则不同；
4. 支持勾选「已完成」，勾上后从一周视图移除，但**保留在列表里**（避免用户以为被误删了）。

**输出**：
- DDL 列表页显示全部 DDL，按截止时间从近到远排；
- 每条显示：名称 + 截止时间 + 一个「完成」勾选框 + 编辑/删除入口；
- 勾选完成后视觉上要有区分（例如加删除线）。

**边界情况**：
- 截止时间已经过去了，用户还在录入 → **允许保存**，但要提示这是过去时间；
- 勾选完成之后想反悔 → 能取消勾选，DDL 回到一周视图；
- DDL 名称重复 → 允许保存，不做去重（用户可能真有两条一样的）。

**为什么这一屏必须有**：这是整个产品里**唯一能让用户「写入」**的地方。
F1 只是把已有的东西显示出来，**没有 F3，用户根本没法把 DDL 弄进系统**，产品就只是个空壳展示页。

> 这一项已展开成用户故事 **US-1**（见第 8 节），配套 4 条验收条件 U1-1 ~ U1-4。

---

### F4｜状态保存在本机（P0）

**这一屏是什么**：不是一屏，是贯穿所有页面的底层机制。

**输入**：用户在 F2 / F3 里保存的每一条数据。

**处理**：
1. 数据存在浏览器本机（localStorage），**不上传任何服务器**；
2. 每次新增 / 修改 / 删除后立刻写回本机，不需要用户点「保存」；
3. 页面重新打开时从本机读回来，让页面回到关闭前的状态。

**输出**：关掉浏览器 → 重新打开 → 课程和 DDL 全部还在。

**边界情况**：
- 用户清空浏览器数据 / 用无痕模式 → **数据会丢失，本期接受这个代价**（见第 2.3 节），不做提示以外的补救；
- 换浏览器或换设备 → 数据不同步，本期接受；
- 数据结构（见第 4 节）要留出将来接数据库的余地。

---

### F5｜课程列表页（P1）

**这一屏是什么**：底部第二个入口，用来管理和查看全部课程。

**输入**：本机保存的课程数据。

**处理**：按星期分组、组内按开始时间排序。

**输出**：能看到所有课程，并能从这一页进入编辑和删除。

**为什么是 P1 而不是 P0**：一周视图里其实已经能看到课程了。
列表页的价值是「方便集中管理」，属于体验优化不是功能必需 ——
时间不够时，编辑和删除的入口可以先只放在一周视图里。

---

### F6｜明确留到本期之后的（P2）

以下功能**写在这里是为了让未来的自己别再重新纠结一遍**，本期一律不做：

- 数据导出 / 导入（换设备时的土办法，等接数据库再说）；
- 学期切换（现在只按「当前这一周」看）；
- 搜索 / 筛选（数据量少，用不上）；
- 深色模式（visual 打磨，等功能跑通）。

---

## 4. 数据与存储

> 这一节回答 research.md 第 6 节留下的问题 4：**存在本地的数据结构长什么样？将来接数据库能不能平滑迁过去？**

### 4.1 设计原则

字段名**现在就按数据库表的风格起**（英文、下划线、带 id 和时间戳），
这样 Day 23 接数据库时，只需要把「读写 localStorage」换成「读写接口」，字段名一个都不用改。
—— 这就是所谓的「平滑迁移」，不是玄学，就是**现在起名的时候多花十分钟**。

### 4.2 课程（course）

```
id            string   唯一标识，本地生成
name          string   课程名，必填
day_of_week   number   星期几，1=周一 … 7=周日，必填
start_time    string   开始时间，格式 "HH:mm"，必填
end_time      string   结束时间，格式 "HH:mm"，必填
location      string   上课地点，可为空
created_at    string   创建时间，ISO 格式
updated_at    string   最后修改时间，ISO 格式
```

**没有 `week` 字段** —— 本期课程一律按「每周重复」处理，不支持单双周（第 2.2 节已砍）。

### 4.3 DDL（ddl）

```
id            string   唯一标识，本地生成
title         string   事项名称，必填
due_date      string   截止日期，格式 "YYYY-MM-DD"，必填
due_time      string   截止时间，格式 "HH:mm"，必填
note          string   备注，可为空
done          boolean  是否已完成，默认 false
created_at    string   创建时间，ISO 格式
updated_at    string   最后修改时间，ISO 格式
```

### 4.4 一个真实例子

```json
{
  "courses": [
    {
      "id": "c_01",
      "name": "高等数学",
      "day_of_week": 1,
      "start_time": "08:00",
      "end_time": "09:40",
      "location": "A101",
      "created_at": "2026-09-19T22:50:00+08:00",
      "updated_at": "2026-09-19T22:50:00+08:00"
    }
  ],
  "ddls": [
    {
      "id": "d_01",
      "title": "交数据结构实验报告",
      "due_date": "2026-09-24",
      "due_time": "23:59",
      "note": "第 3 章之后的题",
      "done": false,
      "created_at": "2026-09-19T22:51:00+08:00",
      "updated_at": "2026-09-19T22:51:00+08:00"
    }
  ]
}
```

### 4.5 为什么课程和 DDL 分成两个列表

**因为它们的重复规则不一样。**
课程是「每周都来」，DDL 是「只来一次」。硬塞进一张表，就得靠字段区分，反而更乱。
分成两个列表之后，两周视图里把它们叠在一起显示即可 —— **存储上分开，展示上合并**。

---

## 5. 页面结构

> 这份 PRD 只管「有几个页面、怎么互相走」，**不画视觉稿**（视觉打磨不是本期的事，见第 2.2 节第 7 条）。

### 5.1 只有两个页面

```
                  ┌─────────────────────────┐
                  │   一周视图（首页）      │  ← 打开就在这，唯一主视图
                  │   F1                    │
                  └───────────┬─────────────┘
                              │ 底部导航切换
                  ┌───────────┴─────────────┐
                  │   课程列表页            │
                  │   F2 / F5              │
                  └─────────────────────────┘

   两个页面都能呼出同一个「表单弹层」：
   ┌───────────────────────────────────────┐
   │  添加课程（F2） / 添加 DDL（F3）      │
   └───────────────────────────────────────┘
```

### 5.2 怎么互相走

| 从哪 | 怎么走 | 到哪 |
| --- | --- | --- |
| 任意地方打开网站 | 直接进入 | 一周视图（F1） |
| 一周视图底部 | 点「课程列表」 | 课程列表页（F5） |
| 任意页面 | 点「+ 添加」 | 表单弹层（F2 或 F3） |
| 表单弹层 | 保存 / 取消 | 回到刚才那一页并刷新内容 |
| 课程/DDL 条目 | 点「编辑」 | 表单弹层，带出原值 |
| 课程/DDL 条目 | 点「删除」 | 弹一次确认 → 删除 |

### 5.3 关于「详情页」的说明

任务清单的降级方案里提到「首页 + 详情」，但**本期不做独立详情页**。
理由：一条课程或一条 DDL 只有 4–5 个字段，弹层里全都看得见。
单独开一页反而多点一次、多返回一次。**字段少的东西，弹层就够了。**

---

## 6. 不做清单（防线）

**这一节是第 2.2 节的浓缩，贴在最后当防线用** ——
Day 5 做技术选型、Day 6-7 写代码时，任何想加的东西先回到这里对一遍。

本期只做三件事：**手动录入 + 一周视图 + 课程与 DDL 叠在一起。**

任何新功能想加进来，先回答一个问题：
> **它是不是直接服务于「这周的 DDL 不丢」？**

答不上来 → 进第 2.2 节的表，本期不做。
答得上来的典型例子：能编辑（录错了要能改，否则 DDL 记错了等于没记）。
答不上来的典型例子：深色模式、社区、导出 Excel。

---

## 7. 验收标准

> **今天交的截图里必须有这一段。**
> 写法要求：每一条都只能用「是」或「否」回答。
> 不能出现「美观」「流畅」「体验好」这类没法验证的词 —— 那种话写了等于没写。

### 7.1 功能验收（P0 部分，共 8 条）

| # | 验收标准 | 是 / 否 |
| --- | --- | --- |
| A1 | 打开页面，不需要登录、不需要输入任何账号，直接看到一周视图 | ☐ |
| A2 | 添加一条课程后，它能出现在一周视图对应的那一天里 | ☐ |
| A3 | 添加一条 DDL 后，它能和课程出现在**同一根时间轴**的那一天里，颜色能区分开 | ☐ |
| A4 | 课程名留空时点击保存，**保存不成功**并有提示 | ☐ |
| A5 | 结束时间早于开始时间时点击保存，**保存不成功**并有提示 | ☐ |
| A6 | 关掉浏览器再重新打开页面，刚才录的课程和 DDL **全部还在** | ☐ |
| A7 | 删除一条记录时会先弹一次确认，点「取消」后数据**没有被删掉** | ☐ |
| A8 | 截止时间已经过去的 DDL **不会消失**，能在过期区域里看到 | ☐ |
| A9 | DDL 截止日期是今天、但截止时刻还没到时，它**不算过期**，仍在正常区域 | ☐ |

### 7.2 边界验收（共 4 条）

| # | 验收标准 | 是 / 否 |
| --- | --- | --- |
| B1 | 一条数据都没有时，一周视图显示引导语和添加按钮，**不是一片空白** | ☐ |
| B2 | 同一天录入两条以上记录时，它们按时间先后排列，**没有重叠或丢失** | ☐ |
| B3 | 同一时间段录入两门课程时，**允许保存**，页面不弹冲突警告 | ☐ |
| B4 | 勾选 DDL「已完成」后，它从一周视图消失，但**仍然能在列表里找到** | ☐ |
| B5 | 勾选「已完成」的 DDL **不会**被当成过期项塞进过期区域 | ☐ |

### 7.3 边界验收（反面 —— 确认「没做」的部分）

> 这一组是用来防我自己偷偷加功能的。**全部应该是「否」。**

| # | 验收标准 | 是 / 否 |
| --- | --- | --- |
| C1 | 页面上能找到「导入课表」「输入学号」这类入口 | ☐（应为否） |
| C2 | 页面上能找到「登录」「注册」「同步」入口 | ☐（应为否） |
| C3 | 浏览器会弹出「是否允许发送通知」的授权请求 | ☐（应为否） |
| C4 | 页面上有设置页或主题切换按钮 | ☐（应为否） |
| C5 | 添加 DDL 的表单里有上传文件的字段 | ☐（应为否） |

### 7.4 交付验收（共 2 条）

| # | 验收标准 | 是 / 否 |
| --- | --- | --- |
| D1 | 交付物是一个可以在手机浏览器直接打开的网页，**不需要安装任何东西** | ☐ |
| D2 | 项目里存在本文件（PRD.md），且它已经提交进仓库 | ☐ |

---

## 8. 用户故事（余力加练）

> 格式：**作为一个 ___，我想要 ___，以便 ___。**
> 前两段说清「谁」和「要什么」，第三段说清「为什么」——
> **第三段才是真正有用的**：答不上来，这个功能就不该做。

### US-1｜把课上听到的 DDL 记下来（对应 F3）

> **作为一个**正在上课、刚听老师说「下周三交实验报告」的学生，
> **我想要**在手机上快速记下这件事的名字和截止时间、不需要注册账号、也不需要联网，
> **以便**我不用去翻微信聊天记录，也不怕当时在低头记笔记没听清，
> 而且这件事会直接出现在我这一周的视图里。

**针对它的验收条件**（同样只回答「是/否」）：

| # | 验收条件 | 是 / 否 |
| --- | --- | --- |
| U1-1 | 从打开页面到 DDL 保存成功，只需要点 1 次「添加」、填 3 个必填字段（事项名称、截止日期、截止时间） | ☐ |
| U1-2 | 全程不需要登录、不需要输入任何账号 | ☐ |
| U1-3 | 手机浏览器打开时，表单里的日期和时间控件能正常唤起手机系统的选择器 | ☐ |
| U1-4 | 保存成功后，这条 DDL 立刻出现在一周视图对应的那一天里 | ☐ |

### US-2｜录错了要能改（对应 F3 的编辑）

> **作为一个**录完 DDL 才发现把「9 月 24 日」敲成「9 月 14 日」的学生，
> **我想要**直接改掉那条记录而不是删掉重录，
> **以便**我不必重新填一遍其他字段 ——
> **记错的 DDL 等于没记，能改是这个功能有用的一部分，不是额外功能。**

### US-3｜过期的 DDL 不要从我眼前消失（对应 F1 的过期区域）

> **作为一个**上周忙着复习、漏看了两个 DDL 的学生，
> **我想要**那些已经过期但还没勾「完成」的 DDL 仍然显示在页面顶部，
> **以便**我知道自己欠着什么，而不是因为它们悄悄消失、我到最后交作业那天才发现。
> （这一条不是多余的：**凡是会「消失」的提醒工具，最后都会让人不敢信它。**）

---

**为什么只写 F3 这一项**：
任务清单的加练要求是「把**一项**功能写成用户故事」。
F3 是本产品里唯一让用户**写入**的功能 —— F1 只是把已有内容显示出来，
没有 F3，用户根本没法把 DDL 弄进系统，产品就只是个空壳。所以从这一项写起最有价值。
（US-2、US-3 是顺着 US-1 牵出来的：它们都属于「让 DDL 真的不丢」这条主线。）

---

## 9. 遗留问题（交给 Day 5）

1. 用什么方式实现「加到手机主屏」？需要哪些 meta 标签？
2. localStorage 的读写放在哪个文件里，才不至于让将来的数据库替换变成大改？
3. Day 7 只有一天做 MVP，第 3 节的 F1–F4 能不能一天做完？如果做不完，第一个砍谁？
   （目前的想法：先砍 F5，再砍 F3 的「编辑」，但**F3 的「添加」无论如何不能砍**。）
4. 一周视图在手机竖屏上，7 天是怎么排的 —— 纵向滚动还是横向滑动？这题留到动手时用真实数据摆一遍再定。
5. US-1 的 U1-3 提到「唤起手机系统的日期选择器」—— `input[type=date]` 在手机浏览器上的表现要实测，不能只信标准。

---

## 10. 遗留问题（Day 9 追加 · 2026-09-24）

> 来源：Day 9 做前端审查时发现「DDL 勾选框只有 18×18px」，
> 由此牵出一个比尺寸更根本的交互问题。当天只修了尺寸，交互改动留到这里。

### 10.1 DDL 的勾选交互：从「点小方框」改成「点整行」？

**问题**：F3「勾选完成」是这个产品的核心动作之一，但勾选框在手机上只有 18×18px。
Day 9 已经把热区撑到 44×44px（达标了），但**视觉上它还是个小方框**，
用户不知道该往哪点。

**为什么值得改**：这个产品的核心场景是 PRD 1.2 的场景 A ——「晚上躺床上看明天有什么」。
单手、躺着、眼睛半睁 —— **去瞄准一个 18px 的方框是最难的姿势**。
点不中的后果不是报错，而是**用户开始怀疑自己**（"是我手指太粗吗"），
点两三次就默默关掉，而且不会告诉你为什么。这类问题不产生报错，只产生流失。

**倾向的方案**：点整行都能切换「完成」状态（热区从 18px 变成整行几百 px）。
改动很小 —— 把 `click` 监听从 `.row-check` 挪到 `.row`。

**已知的坑（动手时必须处理）**：
行内还有「编辑」「删除」两个按钮。事件挪到整行后，
点这两个按钮**不能**同时触发打勾。要么在按钮的 handler 里 `stopPropagation()`，
要么判断 `event.target.closest('.btn-icon')` 时直接返回。
**这两条路都得在真机上试过才算数** —— 模拟点击测不出手指误触。

**其他候选方案**（都不如上面那个）：
- 左滑标记完成（iOS 习惯，但网页上要额外做手势，复杂度和收益不成比）
- 长按（发现性差，用户不会想到去长按）

**什么时候做**：第 3 周，和「手机真机实测」（Day 7 就留下的 U1-3）**一起做**。
理由：只有先在真机上摸过，才能验证「整行可点」是不是真的比「点方框」好 ——
不能靠推测。

**做之前要同步改的地方**：
- PRD 7.1 的功能验收里，F3「勾选完成」的操作描述
- 本节 10.1 本身要划掉，改成「已完成」
