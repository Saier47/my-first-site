# Day 9｜前端审查报告（按 6 条可量化标准）

- 审查日期：2026-09-24
- 审查对象：`index.html`（Day 8 深色科技风主视图）
- 审查方式：
  - **静态分析** `audit-design.js` —— 从 `<style>` 里把变量和 px 值抽出来算
  - **浏览器实测** `audit-design-browser.js` / `audit-page2.html` —— 在真实 Edge 引擎里量渲染后的值
- 为什么必须实测：jsdom 不做布局计算（`getBoundingClientRect` 全是 0），也拿不到计算后的颜色。Day 8 已经在 jsdom 的布局上吃过一次亏。

---

## 一、先说清楚：审查脚本自己也修了 4 个假问题

**这一步比"改了哪几处样式"更重要**——如果拿假问题去改代码，就会把对的代码改坏。
Day 8 的教训原文是：「先分清『被测对象坏了』还是『测量工具坏了』，别急着改对的代码」。

| # | 假问题 | 上一版为什么错 | 修法 |
|---|---|---|---|
| 1 | 「移动端横向溢出，越界元素 2 个」 | 截图时页面还在渲染中途（骨架屏 → 卡片插入的过程中采样） | 等渲染完再量。3 个视口（1280 / 504 / 414）复测**全部 0 个越界** |
| 2 | 「主要按钮 / 勾选框 / 关闭 × 全部 0×0px ❌」 | 这些元素只在**清单视图 / 弹层**里出现。更糟的是选择器写成 `data-view="list"`，而真实标记是 `data-view="courses"`，清单视图**从没打开过** | 改成「找到元素后，把它的隐藏祖先临时显示出来，量完还原」，全程同步 |
| 3 | 「卡片左边缘偏差 1046px ❌」「区标题偏差 20px ❌」 | 7 个卡片本来就**并排**在不同列上，左边缘当然不同；`.day-date` 还故意 `margin-left:auto` 靠右 | 只在**同一列内部**比较，并排除靠右元素（设计意图） |
| 4 | 「`.row-title` 对比度 2.71:1 ❌」 | 隐藏的清单视图元素被选中了。隐藏元素取不到真实背景，脚本退到白底兜底值，于是一个**实际 15.82:1** 的元素被算成 2.71:1 | 只量**真正可见**的元素（自己或祖先 `display:none` 就跳过）；背景兜底改读主题变量 `--bg` |

修完这 4 处后：对齐 **0px 偏差 ✅**，溢出 **0 个 ✅**，`.row-title` 实测 **15.82:1 ✅**。

---

## 二、真问题（实测数值，不是"我觉得"）

### 问题组 A：文字对比度不达标（WCAG 2.1 AA：正文 ≥ 4.5:1）

| 元素 | 实测 color | 实测背景 | 对比度 | 标准 | 状态 |
|---|---|---|---|---|---|
| `.item-loc`（教室号 A101） | `#4e5a75` | `rgb(17,20,28)` | **2.67:1** | 4.5:1 | ❌ 差 1.83 |
| `.day-date`（9/21） | `#4e5a75` | `rgb(15,18,27)` | **2.71:1** | 4.5:1 | ❌ 差 1.79 |
| `.day-empty`（"没有安排"） | `#4e5a75` | `rgb(15,18,27)` | **2.71:1** | 4.5:1 | ❌ 差 1.79 |

> **根因只有一个**：`--faint`（`#4e5a75`）这个颜色对深色底只有 2.67–2.71:1。
> 它当**边框 / 分隔线**用没问题（不要求对比度），当**文字**用就太淡了。
> 三处都是 `color: var(--faint)`。

**已排除的存疑项**：`.row-title` / `.row-meta` 一开始也报不合格，复核后确认是测量工具问题——
`.row-title` 真实 color 是 `rgb(232,236,246)`（很亮），实测 **15.82:1 合格**。不计入修复清单。

### 问题组 B：触达尺寸不足（iOS HIG / Material：≥ 44×44px）

| 元素 | 实测尺寸 | 标准 | 状态 | 根因 |
|---|---|---|---|---|
| `.row-check`（DDL 勾选框） | **18×18px** | 44×44 | ❌ 差 26px | `width/height: 18px`，无扩大热区 |
| `.btn-icon`（编辑/删除） | **36×26px** | 44×44 | ❌ 差 18px（高） | `padding: 2px 5px`，字号 13px |
| `.btn-mini`（+ 课程 / + DDL） | **62×30px** | 44×44 | ❌ 差 14px（高） | `padding: 4px 12px`，字号 12px |
| `.icon-btn`（弹层关闭 ×） | **30×32px** | 44×44 | ❌ 差 14px（高） | `padding: 4px 8px`，字号 24px |
| `.tab`（底部导航） | 625×55px | 44×44 | ✅ 合格 | — |
| `.fab`（+ 添加） | 125×48px | 44×44 | ✅ 合格 | — |
| `.btn-primary`（主要按钮） | 73×46px | 44×44 | ✅ 合格 | — |
| `.btn-ghost`（取消） | 66×46px | 44×44 | ✅ 合格 | — |

> **一句话**：手机上「勾选框 18px」和「编辑/删除 36×26px」是最容易点不中的两个。
> 用户手指触达面积约 44×44px，比这两个元素大一圈。

### 问题组 C：间距节奏（静态分析发现）

| 项目 | 实测 | 标准 | 状态 |
|---|---|---|---|
| px 值落在 4 倍数网格上的比例 | **41 / 63 不在网格**（65%） | ≥ 90% 在网格 | ❌ |
| 偏离值举例 | 13px×5、10px×10、7px×3、18px×4、22px×3 | — | ❌ |
| 卡片之间的 `margin-bottom` | 8px / 7px / 6px **三种混用** | 一种 | ❌ |
| `.item` 内边距 | `9px/10px/9px/12px` | 四向一致或成对 | ⚠️ 不统一 |

### 问题组 D：字号档位（静态分析发现）

| 项目 | 实测 | 建议 | 状态 |
|---|---|---|---|
| 页面字号档数 | **13 档**（10.5 / 11 / 11.5 / 12 / 13 / 13.5 / 14 / 15 / 17 / 19 / 20 / 24 / 26px） | ≤ 4 档 | ❌ 偏多 |
| 相邻档差仅 0.5px 的组 | 10.5↔11、11↔11.5、13↔13.5 | — | ❌ 肉眼分不出 |
| 字重档数 | 400 / 500 / 600 / 700 | ≤ 4 档 | ✅ |
| 标题 / 副标题层级 | 20px / 13px，差 7px | ≥ 2px | ✅ |
| 星期 / 日期层级 | 14px / 12px，差 2px | ≥ 2px | ✅ |

---

## 三、逐条对照 6 条标准

| # | 标准 | 结果 | 说明 |
|---|---|---|---|
| 1 | 对比度 | ❌ **3 处不合格** | 全部因为 `--faint` 被当文字色 |
| 2 | 间距节奏 | ❌ 65% 不在 4 网格 | 卡片下间距 8/7/6px 三种混用 |
| 3 | 对齐 | ✅ 0px 偏差 | 修完假问题后完全对齐 |
| 4 | 层级字号 | ⚠️ 层级合格，字号 13 档偏多 | 相邻档差 0.5px 无意义 |
| 5 | 移动端溢出 | ✅ 3 个视口全 0 越界 | 修复假问题后复测 |
| 6 | 触达尺寸 | ❌ **4 处偏小** | 勾选框 18×18px 最严重 |

**结论：6 条里 3 条合格（对齐 / 溢出 / 层级），3 条有问题（对比度 / 间距 / 触达尺寸）。**

---

## 四、修复清单（按投入产出排序）

| 优先级 | 要修的 | 改哪 | 预期收益 |
|---|---|---|---|
| ⭐⭐⭐ | 4 处触达尺寸 | 给 `.row-check` / `.btn-icon` / `.btn-mini` / `.icon-btn` 加 `min-width`/`min-height: 44px` 或加大 padding（视觉可保持原样，只把热区撑开） | 手机上真正点得中，是「像不像正经产品」感知最强的一处 |
| ⭐⭐⭐ | `--faint` 当文字用的 3 处 | `.item-loc` / `.day-date` / `.day-empty` 的 color 从 `--faint` 换成 `--muted`（`#7d8aa6`，实测 5.3:1+ 会合格） | 教室号、日期、空状态明显更清楚 |
| ⭐⭐ | 卡片间距统一 | `margin-bottom` 的 8 / 7 / 6px 统一成 8px | 视觉节奏整齐 |
| ⭐ | 间距网格 | 13 / 10 / 7 / 18 / 22px 收拢到 4 的倍数 | 逐处调，收益偏细 |
| ⭐ | 字号收敛档位 | 13 档收成 4–5 档 | 动静大（改版面），与「今日不做大改版面」冲突 → 只标记不改 |

---

## 五、修复结果（2026-09-24 完成）

用户拍板**方案 ①：A + B 全修**（对比度 3 处 + 触达尺寸 4 处，共 7 处），原则是**只改 CSS 数值，风格与版面不动**。

### A 组：对比度（3 处）

根因统一：`--faint`（`#4e5a75`）被当**文字色**用，实测只有 2.67–2.71:1。
修法：把这 3 处的 `color` 从 `var(--faint)` 换成 `var(--muted)`（`#7d8aa6`，项目已有变量，不新增）。

| 元素 | 修前 | 修后 | 结果 |
|---|---|---|---|
| `.item-loc`（A101 教室号） | 2.67:1 ❌ | **5.31:1** | ✅ 合格 |
| `.day-date`（9/21 日期） | 2.71:1 ❌ | **5.40:1** | ✅ 合格 |
| `.day-empty`（"没有安排"） | 2.71:1 ❌ | **5.40:1** | ✅ 合格 |

**验证结果：页面上 18 个可见文字元素，全部合格，零不合格。**

> 为什么不选更暗的色：插值算出最暗的达标色是 `#717e9a`（4.52:1），太贴标准线，
> 一点点色差就会掉下去。`--muted` 有 5.3:1 的余量，且本来就是项目里的变量。

### B 组：触达尺寸（4 处）

| 元素 | 修前 | 修后 | 改法 |
|---|---|---|---|
| `.row-check`（勾选框） | 18×18px ❌ | **热区 44×44px** ✅ | 方框本体保持 18px（视觉不变），用 `::before` 铺一层透明 44×44 热区 |
| `.btn-icon`（编辑/删除） | 36×26px ❌ | **44×44px** ✅ | `inline-flex` + `min-width/min-height: 44px` |
| `.btn-mini`（+ 课程 / + DDL） | 62×30px ❌ | **67×44px** ✅ | `min-height: 44px`，padding 从 `4px 12px` 调成 `4px 14px` |
| `.icon-btn`（弹层关闭 ×） | 30×32px ❌ | **44×44px** ✅ | `inline-flex` + `min-width/min-height: 44px` |

**勾选框热区专项验证**（这是最容易出错的一处，用独立的同结构页精确量——因为审查脚本只量元素本体 `getBoundingClientRect()`，不含伪元素）：

```
本体尺寸    = 18×18px（视觉不变）
::before    = 44px × 44px，left=-16px
热区实际范围 = 横 21~65（宽 44px）  纵 72~116（高 44px）  → 合格 ✅
热区右边缘  = 65px
文字左边缘  = 65px
结论：不盖住文字 ✅（右边刚好停在文字前）
```

> **为什么不能居中放大热区**：18px 的方框居中放 44px 热区，会向右溢出 13px，
> 盖住右边 `.row-main` 的标题，导致**点标题变成勾选**——修一个 bug 引出一个更糟的。
> 所以按 `.row` 的实际空间来铺：`16px(左padding) + 18px(方框) + 10px(右gap) = 44px`，
> 顺带把 `.row` 的左 padding 从 13px 调成 16px（也终于落在 4 倍数网格上了）。

### 回归验证

`node check-day8.js` → **通过 65 项，失败 0 项**。7 处修改没有破坏任何 Day 8 已实现的功能。

### 一键复跑

```bash
bash run-audit.sh
```

（脚本里记录了 4 个必须知道的坑：独立 `--user-data-dir`、虚拟时间会跳过 Promise 链、
所以审查页要用「延迟归零」版、`--dump-dom` 输出要截取。下次不用再摸一遍。）

### 修前后对照截图

放在 `docs/day9/`：

| 文件 | 内容 |
|---|---|
| `01-修前-手机.png` | 修前，504px 手机竖屏 |
| `02-修后-手机.png` | 修后，同视口同内容 |
| `01-修前-桌面.png` / `02-修后-桌面.png` | 1280px 桌面宽屏对比 |
| `03-修后-清单视图.png` | 清单页（编辑/删除按钮、勾选框都在这） |

看截图能直接看出：教室号 `A101`、日期 `9/21`、「没有安排」从「几乎看不见」变成「清晰可读」；
清单页的「编辑 / 删除」从挤在一起变成 44×44 的热区，而**勾选框的视觉大小完全没变**。

### 本次仍未做的

- ❌ 间距网格全面收拢（63 个 px 值里还有 38 个不在 4 倍数上）—— 只顺手修了 `.row` 的 padding
- ❌ 字号档位收敛（13 档 → 4–5 档）—— 会改版面结构，与「今日不做大改版面」冲突
- ❌ 卡片 `margin-bottom` 的 8/7/6px 统一 —— 属 C 组，用户本次没选
