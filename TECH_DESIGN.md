# TECH_DESIGN.md｜技术设计：数据从哪来、到哪去

> Day 5 产出 · 2026-09-20
> 上一份文档：PRD.md（Day 4，功能与验收）· 再往前：research.md（Day 3）
> 这份文档只回答一件事：**数据从哪来、经过谁、到哪去 —— 以及技术路线为什么这么选。**

---

## 0. 一句话（本页最重要的一行）

> **数据从用户填的表单来，由 JS 校验后写进浏览器本机的 localStorage；打开页面时再从 localStorage 读出来、排序合并，渲染成一周视图 —— 全程不出用户的设备，本期没有后端，也没有数据库。**

（这行就是今天主任务要交的那句话。第 3 节的数据流图是它的画出来的版本。）

---

## 1. 前后端与数据库分工

### 1.1 通用分工：三套东西各管一段

| 角色 | 在哪跑 | 管什么 | 一句话 |
| --- | --- | --- | --- |
| **前端** | 用户的浏览器里 | 看到的一切：页面、表单、颜色、点击反应 | 「脸」 |
| **后端** | 服务器上 | 看不到的一切：接收请求、执行业务规则、连数据库 | 「厨房」 |
| **数据库** | 服务器上（通常在后端后面） | 只负责把数据存好、按需取出来 | 「仓库」 |

日常一条数据的旅程：前端收用户输入 → 发请求给后端 → 后端写进数据库；要看的时候反过来，数据库 → 后端 → 前端。

### 1.2 本期（Day 7 MVP）的分工：后端整层不存在

| 角色 | 本期由谁干 | 为什么 |
| --- | --- | --- |
| 前端 | **全部**：index.html + store.js + app.js | 交付要求 D1：一个浏览器直接打开的网页 |
| 后端 | **没有这层** | PRD 第 2.2 节砍掉的 8 条里，需要后端撑着的是「导入课表、账号云同步、推送」—— 全砍了，后端就没有活可干。留一层没活干的厨房，只多一台要维护的服务器 |
| 数据库 | **localStorage 代班** | 存取量小（几十条课程/DDL），单设备使用。但字段名已经按数据库表的风格起好了（PRD 第 4 节），Day 23 换真数据库时只换存储、不改字段 |

**判断「要不要后端」的方法**（比记结论有用）：把不做清单翻一遍，看有没有任何一条功能**必须**在服务器上才能完成。本期一条都没有 → 不加。这不是偷懒，是每个后端环节都要付维护成本（部署、域名、宕机），换不来任何一条 PRD 验收项。

---

## 2. 技术路线

### 2.1 选定的路线

> **纯 HTML + CSS + 原生 JavaScript，数据存 localStorage，托管在 GitHub Pages。没有框架、没有构建工具、没有 npm。**

### 2.2 为什么暂时选它（四条理由，全部来自前面几天定下的约束）

1. **交付要求决定形态**：PRD 验收 D1 要求「手机浏览器直接打开、不需要安装任何东西」→ 静态网页天然满足；上框架反而要引入构建打包，交付物就从「一个 html」变成「一套工程」。
2. **功能边界决定规模**：本期只有两个页面 + 一个表单弹层（PRD 第 5 节），没有账号、没有同步、没有推送 → 框架省的那类苦力活（大量 DOM 更新、组件复用、路由）这里几乎没有。
3. **时间预算决定复杂度**：Day 7 只有一天做 MVP。框架的学习成本会直接吃掉写功能的时间 —— 砍功能的顺序已经排到 F5 了（PRD 第 9 节），不能再把预算交给学习工具。
4. **升级路径已经埋好**：数据字段按数据库风格起名（PRD 4.1）、读写层单独一个文件（见 2.3）→ 将来上框架、上后端时，业务逻辑不用推倒重写。**现在不选框架不是没想过框架，是不用预支它的成本。**

### 2.3 MVP 的文件结构（顺手回答 PRD 遗留问题 2）

```
my-first-site/
├── index.html        页面结构 + 样式（本期样式直接内嵌，不单独拆 css 文件）
├── store.js          数据读写层 —— 全项目唯一允许碰 localStorage 的文件
├── app.js            视图层 —— 读数据、排序合并、渲染一周视图、处理表单
├── PRD.md            Day 4
├── TECH_DESIGN.md    Day 5（本文件）
└── research.md       Day 3
```

**一条铁律：localStorage 只出现在 store.js 里。**
app.js 想要数据，只能调 store.js 暴露的「存 / 取 / 改 / 删」函数，不许自己直接摸 localStorage。
这样 Day 23 接数据库时，**只重写 store.js 的内部实现，函数签名不动，app.js 一行不改** —— 这就是把「平滑迁移」从口号变成事实的做法。

---

## 3. 数据流图

```
用户填表单 —— 表单弹层（F2 添加课程 / F3 添加 DDL）
    │  ① 校验：必填项、结束时间晚于开始时间
    ▼
store.js（数据读写层 —— 全项目唯一碰 localStorage 的文件）
    │  ② 增 / 改 / 删 之后立刻写回，不需要用户点保存
    ▼
localStorage（浏览器在本机保管数据）
    │  ③ 打开页面时读回
    ▼
store.js
    │  ④ 过期判定（口径见 PRD 第 1.2 节）  ⑤ 按时间排序、合并、高亮今天
    ▼
一周视图（F1）—— 课程和 DDL 叠在同一根时间轴上

（以上每一步都发生在用户设备的浏览器内部：本期没有后端、没有服务器，
  数据不出这台设备。Day 23 之后，把「localStorage」那一格换成
  「后端接口 + 真数据库」，替换只发生在 store.js 内部，
  图上其他环节一个字不用改。）
```

**读这张图的三个抓手**：

- **唯一的入口是表单**：用户的数据只有一个进来的门（F2/F3），没有第二个 —— 对应 PRD 砍掉「教务系统导入」后的世界。
- **store.js 是数据的中转站**：往上走是「写」（表单 → 存起来），往下走是「读」（读出来 → 变成一周视图），两段路都要经过它 —— 所以它也是唯一需要动手改造的地方。
- **过期判定发生在展示层**：localStorage 里只存原始数据，某条 DDL 算不算过期，是 app.js 拿当前时刻现场算的 —— 存储和展示各管各的。

---

## 4. 方案变化时，哪些文件会受影响（余力加练）

| 如果……（方案变化） | 要动的文件 | 不用动的文件 | 一句话说明 |
| --- | --- | --- | --- |
| Day 23 接云数据库 | store.js（内部重写）、TECH_DESIGN.md | app.js、index.html、PRD.md | 数据层隔离设计就是为这一天准备的 |
| 加深色模式 / 换配色 | index.html | store.js、app.js | 纯样式变化，不碰数据 |
| 加详情页 / 搜索 | app.js、index.html | store.js | 展示层变化；store 的存取函数原样可用 |
| 数据结构加字段（如单双周 week） | store.js（补默认值）、app.js（渲染）、PRD/TECH_DESIGN 同步改 | —— | 字段变化是全链路的事，三个文件都过一遍 |
| 整体换 React/Vue 框架 | index.html、app.js（重写）；store.js 的**函数签名**保留 | —— | 最伤筋动骨的一档 —— 也是当初选原生时明确推迟的账 |

这张表的用法：动手改东西之前先来对一眼，「要动的文件」越少、越靠近表顶部的变化，成本越低。**store.js 越薄越稳 —— 它是全项目改动概率最高的文件，把它做小，就是给未来减负。**

---

## 5. 附：什么是技术栈（余力加练）

**技术栈 = 从界面到存储，一整套互相咬合的技术选择。** 叫「栈」是因为它们像叠罗汉：上层建在下层上，抽掉下面那层，上面的全得重砌。开发一个网页/app，常见的层有这些：

| 层 | 干什么用 | 常见选手 | 优势 / 代价 |
| --- | --- | --- | --- |
| 结构（HTML） | 页面骨架，内容是什么 | 三件套之一 | 零门槛 / 要手写一切 |
| 样式（CSS） | 页面皮肤，长什么样 | 三件套之一 | 同上 |
| 行为（JS） | 页面动作，点了会怎样 | 原生 JS | 同上 |
| 前端框架 | 把手写 DOM 更新、组件复用这类重复劳动自动化 | React、Vue | 省苦力、生态大 / 学习成本 + 构建工具 + 版本焦虑 |
| 后端 | 业务规则、接口、真正需要保密的逻辑 | Node、Python、Java | 能做前端做不到的事 / 要服务器、要部署、要防攻击 |
| 数据库 | 数据的长期家 | MySQL、PostgreSQL、云数据库 | 数据不丢、多端共享 / 要设计表结构、要运维 |
| 部署托管 | 让别人访问得到 | GitHub Pages（静态）、云服务器 | Pages 免费零配置，但只伺候静态页面 |

**每多一层 = 多一分能力 + 多一份维护成本。** 选技术栈的本质不是「哪个最先进」，而是**「本期真的用得到哪几层」**—— 本项目的答案是最底下两层半（结构/样式/行为，托管算半层），后端和数据库两整层，等到 Day 23 有真需求了再叠上去。

---

## 6. 回答 PRD 遗留问题（Day 5 认领的部分）

| PRD 第 9 节的问题 | Day 5 的回答 |
| --- | --- |
| 1. 「加到主屏」需要哪些 meta 标签？ | 最少三个：`<meta name="apple-mobile-web-app-capable" content="yes">`（iOS 全屏）、`<meta name="theme-color" content="…">`（Android 标题栏配色）、`<link rel="apple-touch-icon" href="…">`（图标）。Day 7 写代码时实测「加到主屏后打开是不是独立窗口」 |
| 2. localStorage 读写放哪个文件？ | **store.js，全项目唯一碰它的文件**（见第 2.3 节） |
| 3. 一天做不完先砍谁？ | 维持 PRD 第 9 节的顺序：先砍 F5（课程列表页），再砍 F3 的「编辑」；**F3 的「添加」无论如何不能砍** —— 没有它数据进不来，产品就是空壳 |
| 4. 一周视图手机竖屏怎么排？ | 留给 Day 7，用真实数据摆一遍再定 |
| 5. `input[type=date]` 手机表现？ | 留给 Day 7，实测 US-1 的 U1-3 |

---

## 7. 完成标准对照

- ☑ **TECH_DESIGN.md 已入库** —— commit 之后即达成
- ☑ **技术路线有理由** —— 第 2.2 节，四条理由全部来自 PRD 已定下的约束，不是拍脑袋
- ☑ **数据流图能说清来去** —— 第 3 节：来路是表单，去路是一周视图，中间只经过 store.js 和 localStorage
